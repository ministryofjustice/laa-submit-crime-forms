<% content_for?(:page_title) ? yield(:page_title) : assess_fallback_title %>

<% content_for(:header) do %>
  <%= govuk_header(service_name: t('assess.service.name'), classes: ["app-banner-#{HostEnv.env_name}"]) %>
<% end %>

<% content_for(:content) do %>
  <%= render partial: 'flash_banner' %>
  <%= yield %>
<% end %>

<%= render template: 'layouts/assess_govuk_template', layout: false %>
