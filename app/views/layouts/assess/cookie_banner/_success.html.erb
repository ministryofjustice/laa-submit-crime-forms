<%= turbo_frame_tag :removable do %>
  <div class="govuk-cookie-banner " data-nosnippet role="region" aria-label="Cookies on <%=Rails.configuration.x.application.name %>">
    <div class="govuk-cookie-banner__message govuk-width-container">
      <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
          <div class="govuk-cookie-banner__content">
            <p class="govuk-body">
              <%= @analytics_cookies_accepted ? t('layouts.assess_application.cookies.confirmation_accepted') : t('layouts.assess_application.cookies.confirmation_rejected') %>
              <%= t('layouts.assess_application.cookies.link_to_settings', href: assess_about_cookies_path).html_safe %>
            </p>
          </div>
        </div>
      </div>
      <div class="govuk-button-group">
        <%= govuk_link_to "Hide this message", assess_about_update_cookies_path(hide_banner: true), class: 'govuk-button', 'data-module': 'govuk-button' %>
      </div>
    </div>
  </div>
<% end %>
