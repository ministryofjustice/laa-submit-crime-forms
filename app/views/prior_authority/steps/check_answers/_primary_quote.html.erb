<%= govuk_summary_card(title: section.title, actions: section.actions) do |card|
      govuk_summary_list(rows: section.rows)
        .concat(
          render('primary_quote_totals', section: section)
        )
    end %>

