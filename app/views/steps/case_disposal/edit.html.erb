<% title t('.page_title') %>
<% step_header %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-l"><%= t('.heading') %></h1>
    <h2 class="govuk-heading-s govuk-!-margin-bottom-6"><%= t '.subheading' %></h2>

    <%= govuk_error_summary(@form_object) %>

    <%= step_form @form_object do |f| %>
      <% @form_object.choices.each do |name, pleas| %>
        <%= f.govuk_radio_buttons_fieldset name, legend: { size: 'm' } do %>
          <% pleas.each_with_index do |plea, index| %>
            <span id="<%= plea %>"></span>
            <%= f.govuk_radio_button :plea, plea.to_s, link_errors: index.zero? do %>
              <% if plea.requires_date_field? %>
                <%= f.govuk_date_field :"#{plea}_date", maxlength_enabled: true, legend: { size: 's', class: 'govuk-date-legend-non-bold' } %>
              <% end %>
            <% end %>
          <% end %>
        <% end %>
      <% end %>

      <%= f.continue_button %>
    <% end %>
  </div>
</div>
