feature_flags:
  omniauth_test_mode:
    local: <%= ENV.fetch("OMNIAUTH_TEST_MODE", false) %>
    development: true
    uat: false
    production: false # must never be enabled in the live service
  manual_sync_trigger:
    local: true
    development: true
    uat: false
    production: false
